
#pragma once

#include <string>
#include <vector>
#include <iostream>

using namespace std;

// sep: 分割字符
// 返回分割后的字符串数组
std::vector<std::string> split(const std::string& input, char sep);
void printVec(const vector<int> &v);